# Phase 1.5: Tab 互動強化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 將 TabBar 升級為完整 tab 管理介面：固定/鎖定、keep-alive 快取、拖曳排序、溢出箭頭、右鍵選單。

**Architecture:** 五個獨立切片逐步疊加。Tab 模型擴充為基礎（#1），TerminalView 拆分 + Keep-Alive（#2）、拖曳排序（#3）、溢出箭頭（#4）、右鍵選單（#5）在其上獨立開發。TabContent 從單一 activeTab 渲染改為 LRU cache pool。

**Tech Stack:** React 19 / Zustand 5 / @dnd-kit (core + sortable) / @floating-ui/react / Tailwind 4 / Vitest / Phosphor Icons

**Spec:** `docs/superpowers/specs/2026-03-21-phase1.5-tab-interactions-design.md`

---

## File Structure

### 新建檔案

| 檔案 | 職責 |
|------|------|
| `spa/src/hooks/useTerminal.ts` | xterm instance 初始化 + addon 管理 + IME 處理 |
| `spa/src/hooks/useTerminalWs.ts` | WS 連線建立 + data relay + reconnect 偵測 |
| `spa/src/hooks/useTabAlivePool.ts` | LRU keep-alive pool 管理 |
| `spa/src/hooks/useScrollOverflow.ts` | 捲動溢出偵測（ResizeObserver + scroll） |
| `spa/src/components/SortableTab.tsx` | dnd-kit sortable 的單一 tab 元件 |
| `spa/src/components/TabContextMenu.tsx` | 右鍵選單元件 |

### 修改檔案

| 檔案 | 改動摘要 |
|------|----------|
| `spa/src/types/tab.ts` | Tab interface 加 `pinned?`, `locked?`；工廠函式補預設值 |
| `spa/src/stores/useTabStore.ts` | 加 pinTab/unpinTab/lockTab/unlockTab；removeTab/dismissTab 加 locked 檢查；persist v1→v2 |
| `spa/src/stores/useUISettingsStore.ts` | 加 keepAliveCount, keepAlivePinned, terminalSettingsVersion + bump |
| `spa/src/components/TabBar.tsx` | 重寫：dnd-kit 雙區 + pinned 縮小 + 溢出箭頭 + 中鍵關閉 |
| `spa/src/components/TabContent.tsx` | 改為 LRU cache pool 渲染 |
| `spa/src/components/TerminalView.tsx` | 拆出 hooks，本體縮到 ~100 行 |
| `spa/src/components/SettingsPanel.tsx` | 加 keepAliveCount 設定；onTerminalReconnect → bumpTerminalSettingsVersion |
| `spa/src/App.tsx` | 移除 onTerminalReconnect 空函式；TabBar 新增 props |

---

## Task 1: Tab 模型擴充

**Branch:** `feat/phase1.5-tab-model`

**Files:**
- Modify: `spa/src/types/tab.ts`
- Modify: `spa/src/stores/useTabStore.ts`
- Modify: `spa/src/stores/useUISettingsStore.ts`
- Test: `spa/src/types/tab.test.ts`
- Test: `spa/src/stores/useTabStore.test.ts`
- Test: `spa/src/stores/useUISettingsStore.test.ts`

### Task 1.1: Tab interface + 工廠函式

- [ ] **Step 1: 寫 tab.test.ts 的 pinned/locked 測試**

在 `spa/src/types/tab.test.ts` 末尾加入：

```typescript
describe('pinned / locked defaults', () => {
  it('createSessionTab defaults pinned=false, locked=false', () => {
    const tab = createSessionTab({ label: 'x', hostId: 'local', sessionName: 'x' })
    expect(tab.pinned).toBe(false)
    expect(tab.locked).toBe(false)
  })

  it('createEditorTab defaults pinned=false, locked=false', () => {
    const tab = createEditorTab({ label: 'x', hostId: 'local', filePath: '/tmp/x' })
    expect(tab.pinned).toBe(false)
    expect(tab.locked).toBe(false)
  })

  it('createTab defaults pinned=false, locked=false', () => {
    const tab = createTab({ type: 'custom', label: 'x', hostId: 'local' })
    expect(tab.pinned).toBe(false)
    expect(tab.locked).toBe(false)
  })
})
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `cd spa && npx vitest run src/types/tab.test.ts`
Expected: FAIL — `pinned` 和 `locked` 為 `undefined`

- [ ] **Step 3: 修改 Tab interface + 工廠函式**

`spa/src/types/tab.ts` — Tab interface 加入：

```typescript
export interface Tab {
  id: string
  type: string
  label: string
  icon: string
  hostId: string
  viewMode?: string
  data: Record<string, unknown>
  pinned: boolean    // 新增
  locked: boolean    // 新增
}
```

三個工廠函式的 return 都加 `pinned: false, locked: false`。

- [ ] **Step 4: 執行測試確認通過**

Run: `cd spa && npx vitest run src/types/tab.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add spa/src/types/tab.ts spa/src/types/tab.test.ts
git commit -m "feat(tab): add pinned/locked fields to Tab interface"
```

### Task 1.2: useTabStore — pin/unpin/lock/unlock + locked 檢查

- [ ] **Step 1: 寫 useTabStore.test.ts 測試**

建立或擴充 `spa/src/stores/useTabStore.test.ts`：

```typescript
import { describe, it, expect, beforeEach } from 'vitest'
import { useTabStore } from './useTabStore'
import { createSessionTab } from '../types/tab'

function resetStore() {
  useTabStore.setState({
    tabs: {},
    tabOrder: [],
    activeTabId: null,
    dismissedSessions: [],
  })
}

function addTestTab(name: string, overrides?: Partial<ReturnType<typeof createSessionTab>>) {
  const tab = { ...createSessionTab({ label: name, hostId: 'local', sessionName: name }), ...overrides }
  useTabStore.getState().addTab(tab)
  return tab
}

describe('pinTab / unpinTab', () => {
  beforeEach(resetStore)

  it('pinTab sets pinned=true, locked=true, moves to pinned zone end', () => {
    const a = addTestTab('a')
    const b = addTestTab('b')
    const c = addTestTab('c')
    useTabStore.getState().pinTab(b.id)

    const state = useTabStore.getState()
    expect(state.tabs[b.id].pinned).toBe(true)
    expect(state.tabs[b.id].locked).toBe(true)
    // b should be at index 0 (pinned zone), a and c after
    expect(state.tabOrder).toEqual([b.id, a.id, c.id])
  })

  it('unpinTab sets pinned=false, moves to normal zone start, keeps locked', () => {
    const a = addTestTab('a')
    const b = addTestTab('b')
    useTabStore.getState().pinTab(a.id)
    useTabStore.getState().unpinTab(a.id)

    const state = useTabStore.getState()
    expect(state.tabs[a.id].pinned).toBe(false)
    expect(state.tabs[a.id].locked).toBe(true) // locked preserved
    // a should be at start of normal zone (after any pinned)
    expect(state.tabOrder[0]).toBe(a.id)
  })
})

describe('lockTab / unlockTab', () => {
  beforeEach(resetStore)

  it('lockTab sets locked=true', () => {
    const a = addTestTab('a')
    useTabStore.getState().lockTab(a.id)
    expect(useTabStore.getState().tabs[a.id].locked).toBe(true)
  })

  it('unlockTab sets locked=false', () => {
    const a = addTestTab('a')
    useTabStore.getState().lockTab(a.id)
    useTabStore.getState().unlockTab(a.id)
    expect(useTabStore.getState().tabs[a.id].locked).toBe(false)
  })

  it('unlockTab on pinned tab is no-op', () => {
    const a = addTestTab('a')
    useTabStore.getState().pinTab(a.id)
    useTabStore.getState().unlockTab(a.id)
    expect(useTabStore.getState().tabs[a.id].locked).toBe(true) // still locked
  })
})

describe('locked tab blocks close', () => {
  beforeEach(resetStore)

  it('removeTab on locked tab is no-op', () => {
    const a = addTestTab('a')
    useTabStore.getState().lockTab(a.id)
    useTabStore.getState().removeTab(a.id)
    expect(useTabStore.getState().tabs[a.id]).toBeDefined()
  })

  it('dismissTab on locked tab is no-op', () => {
    const a = addTestTab('a')
    useTabStore.getState().lockTab(a.id)
    useTabStore.getState().dismissTab(a.id)
    expect(useTabStore.getState().tabs[a.id]).toBeDefined()
  })

  it('removeTab on unlocked tab still works', () => {
    const a = addTestTab('a')
    useTabStore.getState().removeTab(a.id)
    expect(useTabStore.getState().tabs[a.id]).toBeUndefined()
  })
})
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `cd spa && npx vitest run src/stores/useTabStore.test.ts`
Expected: FAIL — pinTab, unpinTab, lockTab, unlockTab 不存在

- [ ] **Step 3: 實作 store 方法**

`spa/src/stores/useTabStore.ts` — TabState interface 加入：

```typescript
pinTab: (tabId: string) => void
unpinTab: (tabId: string) => void
lockTab: (tabId: string) => void
unlockTab: (tabId: string) => void
```

實作：

```typescript
pinTab: (tabId) =>
  set((state) => {
    const tab = state.tabs[tabId]
    if (!tab) return state
    const updated = { ...tab, pinned: true, locked: true }
    const newOrder = state.tabOrder.filter((id) => id !== tabId)
    // Insert at end of pinned zone
    const firstNormalIdx = newOrder.findIndex((id) => !state.tabs[id]?.pinned)
    const insertIdx = firstNormalIdx === -1 ? newOrder.length : firstNormalIdx
    newOrder.splice(insertIdx, 0, tabId)
    return { tabs: { ...state.tabs, [tabId]: updated }, tabOrder: newOrder }
  }),

unpinTab: (tabId) =>
  set((state) => {
    const tab = state.tabs[tabId]
    if (!tab || !tab.pinned) return state
    const updated = { ...tab, pinned: false } // locked preserved
    const newOrder = state.tabOrder.filter((id) => id !== tabId)
    // Insert at start of normal zone (after last pinned)
    const firstNormalIdx = newOrder.findIndex((id) => !state.tabs[id]?.pinned)
    const insertIdx = firstNormalIdx === -1 ? newOrder.length : firstNormalIdx
    newOrder.splice(insertIdx, 0, tabId)
    return { tabs: { ...state.tabs, [tabId]: updated }, tabOrder: newOrder }
  }),

lockTab: (tabId) =>
  set((state) => {
    if (!state.tabs[tabId]) return state
    return { tabs: { ...state.tabs, [tabId]: { ...state.tabs[tabId], locked: true } } }
  }),

unlockTab: (tabId) =>
  set((state) => {
    const tab = state.tabs[tabId]
    if (!tab || tab.pinned) return state // pinned tab can't be unlocked
    return { tabs: { ...state.tabs, [tabId]: { ...tab, locked: false } } }
  }),
```

修改 `removeTab` 和 `dismissTab`，在最前面加 locked 檢查：

```typescript
removeTab: (tabId) =>
  set((state) => {
    if (!state.tabs[tabId]) return state
    if (state.tabs[tabId].locked) return state  // 新增
    // ... rest unchanged
  }),

dismissTab: (tabId) =>
  set((state) => {
    const tab = state.tabs[tabId]
    if (!tab) return state
    if (tab.locked) return state  // 新增
    // ... rest unchanged
  }),
```

- [ ] **Step 4: 執行測試確認通過**

Run: `cd spa && npx vitest run src/stores/useTabStore.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add spa/src/stores/useTabStore.ts spa/src/stores/useTabStore.test.ts
git commit -m "feat(store): add pin/unpin/lock/unlock with locked close guard"
```

### Task 1.3: Persist migration v1→v2

- [ ] **Step 1: 寫 migration 測試**

在 `spa/src/stores/useTabStore.test.ts` 加入：

```typescript
describe('persist migration v1→v2', () => {
  it('adds pinned=false, locked=false to old tabs', () => {
    const { migrate } = useTabStore.persist.getOptions()
    const old = {
      tabs: {
        't1': { id: 't1', type: 'session', label: 'x', icon: 'Terminal', hostId: 'local', data: {} },
      },
      tabOrder: ['t1'],
      activeTabId: 't1',
      dismissedSessions: [],
    }
    const result = migrate!(old, 1) as any
    expect(result.tabs.t1.pinned).toBe(false)
    expect(result.tabs.t1.locked).toBe(false)
  })

  it('preserves existing pinned/locked values', () => {
    const { migrate } = useTabStore.persist.getOptions()
    const old = {
      tabs: {
        't1': { id: 't1', type: 'session', label: 'x', icon: 'Terminal', hostId: 'local', data: {}, pinned: true, locked: true },
      },
      tabOrder: ['t1'],
      activeTabId: 't1',
      dismissedSessions: [],
    }
    const result = migrate!(old, 1) as any
    expect(result.tabs.t1.pinned).toBe(true)
    expect(result.tabs.t1.locked).toBe(true)
  })
})
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `cd spa && npx vitest run src/stores/useTabStore.test.ts`
Expected: FAIL — migration 不處理 version 1

- [ ] **Step 3: 更新 persist config**

`spa/src/stores/useTabStore.ts` — persist options 改為：

```typescript
{
  name: 'tbox-tabs',
  version: 2,
  migrate: (persisted: any, version: number) => {
    if (version < 1) {
      // v0→v1: type union → open type + data bag (existing)
      const tabs: Record<string, any> = persisted.tabs ?? {}
      const migrated: Record<string, any> = {}
      for (const [id, tab] of Object.entries(tabs) as [string, any][]) {
        if (tab.type === 'terminal' || tab.type === 'stream') {
          const { sessionName, ...rest } = tab
          migrated[id] = { ...rest, type: 'session', viewMode: tab.type === 'stream' ? 'stream' : 'terminal', data: { sessionName } }
        } else if (tab.type === 'editor') {
          const { filePath, isDirty, ...rest } = tab
          migrated[id] = { ...rest, type: 'editor', data: { filePath, isDirty: isDirty ?? false } }
        } else {
          migrated[id] = { ...tab, data: tab.data ?? {} }
        }
      }
      persisted = { ...persisted, tabs: migrated }
    }
    if (version < 2) {
      // v1→v2: add pinned/locked defaults (immutable — spread to avoid mutation)
      const tabs: Record<string, any> = persisted.tabs ?? {}
      const migrated: Record<string, any> = {}
      for (const [id, tab] of Object.entries(tabs) as [string, any][]) {
        migrated[id] = { ...tab, pinned: tab.pinned ?? false, locked: tab.locked ?? false }
      }
      persisted = { ...persisted, tabs: migrated }
    }
    return persisted
  },
  // partialize unchanged
}
```

- [ ] **Step 4: 執行測試確認通過**

Run: `cd spa && npx vitest run src/stores/useTabStore.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add spa/src/stores/useTabStore.ts spa/src/stores/useTabStore.test.ts
git commit -m "feat(store): persist migration v1→v2 adds pinned/locked defaults"
```

### Task 1.4: UISettingsStore 新增欄位

- [ ] **Step 1: 擴充既有 UISettingsStore 測試**

`spa/src/stores/useUISettingsStore.test.ts` 已存在。先更新既有的 `beforeEach` / `resetStore` 函式加入新欄位（`keepAliveCount: 0, keepAlivePinned: false, terminalSettingsVersion: 0`），再在末尾加入新測試：

```typescript
import { describe, it, expect, beforeEach } from 'vitest'
import { useUISettingsStore } from './useUISettingsStore'

function resetStore() {
  useUISettingsStore.setState({
    terminalRevealDelay: 300,
    terminalRenderer: 'webgl',
    keepAliveCount: 0,
    keepAlivePinned: false,
    terminalSettingsVersion: 0,
  })
}

describe('keep-alive settings', () => {
  beforeEach(resetStore)

  it('keepAliveCount defaults to 0', () => {
    expect(useUISettingsStore.getState().keepAliveCount).toBe(0)
  })

  it('keepAlivePinned defaults to false', () => {
    expect(useUISettingsStore.getState().keepAlivePinned).toBe(false)
  })

  it('setKeepAliveCount updates value', () => {
    useUISettingsStore.getState().setKeepAliveCount(3)
    expect(useUISettingsStore.getState().keepAliveCount).toBe(3)
  })

  it('setKeepAlivePinned updates value', () => {
    useUISettingsStore.getState().setKeepAlivePinned(true)
    expect(useUISettingsStore.getState().keepAlivePinned).toBe(true)
  })
})

describe('terminalSettingsVersion', () => {
  beforeEach(resetStore)

  it('defaults to 0', () => {
    expect(useUISettingsStore.getState().terminalSettingsVersion).toBe(0)
  })

  it('bumpTerminalSettingsVersion increments', () => {
    useUISettingsStore.getState().bumpTerminalSettingsVersion()
    expect(useUISettingsStore.getState().terminalSettingsVersion).toBe(1)
    useUISettingsStore.getState().bumpTerminalSettingsVersion()
    expect(useUISettingsStore.getState().terminalSettingsVersion).toBe(2)
  })
})
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `cd spa && npx vitest run src/stores/useUISettingsStore.test.ts`
Expected: FAIL — keepAliveCount 等欄位不存在

- [ ] **Step 3: 擴充 useUISettingsStore**

`spa/src/stores/useUISettingsStore.ts` — interface 加入：

```typescript
keepAliveCount: number
setKeepAliveCount: (n: number) => void
keepAlivePinned: boolean
setKeepAlivePinned: (v: boolean) => void
terminalSettingsVersion: number
bumpTerminalSettingsVersion: () => void
```

store 實作加入：

```typescript
keepAliveCount: 0,
setKeepAliveCount: (n) => set({ keepAliveCount: n }),
keepAlivePinned: false,
setKeepAlivePinned: (v) => set({ keepAlivePinned: v }),
terminalSettingsVersion: 0,
bumpTerminalSettingsVersion: () => set((s) => ({ terminalSettingsVersion: s.terminalSettingsVersion + 1 })),
```

- [ ] **Step 4: 執行測試確認通過**

Run: `cd spa && npx vitest run src/stores/useUISettingsStore.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add spa/src/stores/useUISettingsStore.ts spa/src/stores/useUISettingsStore.test.ts
git commit -m "feat(store): add keepAlive + terminalSettingsVersion to UISettings"
```

### Task 1.5: 修正既有測試 + 全套測試通過

- [ ] **Step 1: 執行全部測試找出因 pinned/locked 造成的失敗**

Run: `cd spa && npx vitest run`
Expected: 部分測試可能因 Tab interface 變更而失敗（mock data 缺少 pinned/locked）

- [ ] **Step 2: 修正所有失敗的測試 mock data**

在每個使用 mock Tab 物件的測試檔案中，補上 `pinned: false, locked: false`。例如 `TabBar.test.tsx` 的 mockTabs。

- [ ] **Step 3: 執行全部測試確認通過**

Run: `cd spa && npx vitest run`
Expected: ALL PASS

- [ ] **Step 4: Lint 檢查**

Run: `cd spa && npm run lint`
Expected: 無錯誤

- [ ] **Step 5: Commit**

```bash
git add -u
git commit -m "fix(test): update mock data for pinned/locked fields"
```

---

## Task 2: TerminalView 拆分 + Keep-Alive 機制

**Branch:** `feat/phase1.5-keep-alive`（基於 Task 1 的 branch 合併後）

**Files:**
- Create: `spa/src/hooks/useTerminal.ts`
- Create: `spa/src/hooks/useTerminalWs.ts`
- Create: `spa/src/hooks/useTabAlivePool.ts`
- Modify: `spa/src/components/TerminalView.tsx`
- Modify: `spa/src/components/TabContent.tsx`
- Modify: `spa/src/components/SettingsPanel.tsx`
- Modify: `spa/src/App.tsx`
- Test: `spa/src/hooks/useTabAlivePool.test.ts`
- Test: `spa/src/components/TabContent.test.tsx`（擴充）

### Task 2.1: useTerminal hook 拆出

- [ ] **Step 1: 建立 useTerminal.ts**

`spa/src/hooks/useTerminal.ts` — 從 TerminalView.tsx 抽出 xterm 初始化邏輯：

```typescript
import { useEffect, useRef } from 'react'
import { Terminal } from '@xterm/xterm'
import { FitAddon } from '@xterm/addon-fit'
import { WebglAddon } from '@xterm/addon-webgl'
import { Unicode11Addon } from '@xterm/addon-unicode11'
import { WebLinksAddon } from '@xterm/addon-web-links'
import { useUISettingsStore } from '../stores/useUISettingsStore'

export interface UseTerminalResult {
  termRef: React.RefObject<Terminal | null>
  fitAddonRef: React.RefObject<FitAddon | null>
  containerRef: React.RefObject<HTMLDivElement | null>
}

/**
 * xterm instance 的生命週期綁定 component mount/unmount。
 * wsUrl 變更或 settings 變更導致的重建由 TabContent 的 pool key
 * (`key={id}-${poolVersion}`) 觸發 remount，不在此 hook 處理。
 */
export function useTerminal(): UseTerminalResult {
  const containerRef = useRef<HTMLDivElement>(null)
  const termRef = useRef<Terminal | null>(null)
  const fitAddonRef = useRef<FitAddon | null>(null)

  useEffect(() => {
    if (!containerRef.current) return

    const term = new Terminal({
      theme: { background: '#0a0a1a', foreground: '#e0e0e0' },
      fontSize: 14,
      fontFamily: 'Menlo, Monaco, monospace',
      cursorBlink: true,
      macOptionClickForcesSelection: true,
      rightClickSelectsWord: true,
    })

    const fitAddon = new FitAddon()
    term.loadAddon(fitAddon)
    term.open(containerRef.current)
    fitAddonRef.current = fitAddon
    termRef.current = term

    const renderer = useUISettingsStore.getState().terminalRenderer
    if (renderer === 'webgl') {
      try { term.loadAddon(new WebglAddon()) } catch { /* fallback to DOM */ }
    }
    try {
      term.loadAddon(new Unicode11Addon())
      term.unicode.activeVersion = '11'
    } catch { /* fallback */ }
    try { term.loadAddon(new WebLinksAddon()) } catch { /* non-critical */ }

    requestAnimationFrame(() => fitAddon.fit())

    const container = containerRef.current
    let rafId = 0
    const observer = new ResizeObserver(() => {
      cancelAnimationFrame(rafId)
      rafId = requestAnimationFrame(() => fitAddon.fit())
    })
    observer.observe(container)

    // Suppress browser context menu on terminal
    const handleContextMenu = (e: MouseEvent) => e.preventDefault()
    container.addEventListener('contextmenu', handleContextMenu)

    return () => {
      cancelAnimationFrame(rafId)
      observer.disconnect()
      container.removeEventListener('contextmenu', handleContextMenu)
      term.dispose()
      fitAddonRef.current = null
      termRef.current = null
    }
  }, [])

  return { termRef, fitAddonRef, containerRef }
}
```

- [ ] **Step 2: 建立 useTerminalWs.ts**

`spa/src/hooks/useTerminalWs.ts` — WS 連線 + data relay + 鍵盤處理：

```typescript
import { useEffect, useRef } from 'react'
import type { Terminal } from '@xterm/xterm'
import type { FitAddon } from '@xterm/addon-fit'
import { connectTerminal } from '../lib/ws'
import { useUISettingsStore } from '../stores/useUISettingsStore'

interface UseTerminalWsOpts {
  wsUrl: string
  termRef: React.RefObject<Terminal | null>
  fitAddonRef: React.RefObject<FitAddon | null>
  containerRef: React.RefObject<HTMLDivElement | null>
  onReady: () => void
  onDisconnect: () => void
  onReconnect: () => void
}

export function useTerminalWs({ wsUrl, termRef, fitAddonRef, containerRef, onReady, onDisconnect, onReconnect }: UseTerminalWsOpts) {
  const connRef = useRef<ReturnType<typeof connectTerminal> | null>(null)
  const revealDelayRef = useRef(useUISettingsStore.getState().terminalRevealDelay)

  useEffect(() => {
    return useUISettingsStore.subscribe((s) => { revealDelayRef.current = s.terminalRevealDelay })
  }, [])

  useEffect(() => {
    const term = termRef.current
    const container = containerRef.current
    if (!term || !container) return

    let revealed = false
    const reveal = () => {
      if (revealed) return
      revealed = true
      onReady()
      term.focus()
    }

    const conn = connectTerminal(
      wsUrl,
      (data) => {
        term.write(new Uint8Array(data))
        if (!revealed) setTimeout(reveal, revealDelayRef.current)
      },
      () => onDisconnect(),
      () => {
        onReconnect()
        if (revealed) onReady()
        fitAddonRef.current?.fit()
        conn.resize(term.cols, term.rows)
      },
    )
    connRef.current = conn

    const ta = container.querySelector('.xterm-helper-textarea')

    // Shift+Enter → \n
    let shiftEnterHandled = false
    const handleShiftEnter = (ev: Event) => {
      const ke = ev as KeyboardEvent
      if (ke.key === 'Enter' && ke.shiftKey && !ke.ctrlKey && !ke.metaKey) {
        ke.stopPropagation()
        ke.preventDefault()
        shiftEnterHandled = true
        conn.send('\n')
      }
    }
    container.addEventListener('keydown', handleShiftEnter, true)

    // IME duplicate guard
    let lastComposedSent = ''
    const handleCompositionStart = () => { lastComposedSent = '' }
    ta?.addEventListener('compositionstart', handleCompositionStart)

    term.onData((data) => {
      if (shiftEnterHandled && data === '\r') { shiftEnterHandled = false; return }
      shiftEnterHandled = false
      const isComposed = data.length > 1 && data.charCodeAt(0) !== 0x1b
      if (isComposed && data === lastComposedSent) return
      if (isComposed) lastComposedSent = data
      else lastComposedSent = ''
      conn.send(data)
    })
    term.onResize(({ cols, rows }) => conn.resize(cols, rows))

    return () => {
      container.removeEventListener('keydown', handleShiftEnter, true)
      ta?.removeEventListener('compositionstart', handleCompositionStart)
      conn.close()
      connRef.current = null
    }
  }, [wsUrl, termRef, fitAddonRef, containerRef, onReady, onDisconnect, onReconnect])

  return connRef
}
```

- [ ] **Step 3: 改寫 TerminalView.tsx 使用新 hooks**

`spa/src/components/TerminalView.tsx` 縮減為 ~100 行，組合 hooks + overlay UI + visibility 管理：

```typescript
import { useEffect, useState, useRef, useCallback } from 'react'
import { useTerminal } from '../hooks/useTerminal'
import { useTerminalWs } from '../hooks/useTerminalWs'
import '@xterm/xterm/css/xterm.css'

interface Props {
  wsUrl: string
  visible?: boolean
  connectingMessage?: string
}

export default function TerminalView({ wsUrl, visible = true, connectingMessage }: Props) {
  const { termRef, fitAddonRef, containerRef } = useTerminal()
  const [ready, setReady] = useState(false)
  const [disconnected, setDisconnected] = useState(false)
  const prevVisible = useRef(visible)

  const handleReady = useCallback(() => { setReady(true) }, [])
  const handleDisconnect = useCallback(() => { setDisconnected(true) }, [])
  const handleReconnect = useCallback(() => { setDisconnected(false) }, [])

  const connRef = useTerminalWs({
    wsUrl,
    termRef,
    fitAddonRef,
    containerRef,
    onReady: handleReady,
    onDisconnect: handleDisconnect,
    onReconnect: handleReconnect,
  })

  // Reset state on wsUrl change
  useEffect(() => {
    setReady(false)
    setDisconnected(false)
  }, [wsUrl])

  // Re-show overlay + refit when becoming visible after being hidden
  useEffect(() => {
    if (visible && !prevVisible.current) {
      setReady(false)
      requestAnimationFrame(() => {
        fitAddonRef.current?.fit()
        const term = termRef.current
        const conn = connRef.current
        if (term && conn) conn.resize(term.cols, term.rows)
      })
      const timer = setTimeout(() => {
        setReady(true)
        termRef.current?.focus()
      }, 500)
      prevVisible.current = visible
      return () => clearTimeout(timer)
    }
    prevVisible.current = visible
  }, [visible, termRef, fitAddonRef, connRef])

  const showOverlay = !ready || disconnected

  return (
    <div className="w-full h-full relative" style={{ background: '#0a0a1a' }}>
      <div ref={containerRef} className="w-full h-full" />
      <div
        data-testid="terminal-overlay"
        className="absolute inset-0 flex items-center justify-center pointer-events-none"
        style={{
          background: disconnected ? 'rgba(10, 10, 26, 0.5)' : '#0a0a1a',
          opacity: showOverlay ? 1 : 0,
          transition: 'opacity 0.3s ease-out',
        }}
      >
        <span className="text-gray-500 text-sm" style={{ animation: 'breathing 2s ease-in-out infinite' }}>
          {disconnected ? 'reconnecting...' : (connectingMessage || 'connecting...')}
        </span>
        <style>{`@keyframes breathing { 0%, 100% { opacity: 0.3; } 50% { opacity: 1; } }`}</style>
      </div>
    </div>
  )
}
```

- [ ] **Step 4: 執行既有 TerminalView 測試確認不破壞**

Run: `cd spa && npx vitest run src/components/TerminalView.test.tsx`
Expected: PASS（行為不變，只是拆分）

- [ ] **Step 5: Commit**

```bash
git add spa/src/hooks/useTerminal.ts spa/src/hooks/useTerminalWs.ts spa/src/components/TerminalView.tsx
git commit -m "refactor(terminal): extract useTerminal + useTerminalWs hooks"
```

### Task 2.2: useTabAlivePool hook

- [ ] **Step 1: 寫 useTabAlivePool 測試**

`spa/src/hooks/useTabAlivePool.test.ts`：

```typescript
import { describe, it, expect, beforeEach } from 'vitest'
import { renderHook, act } from '@testing-library/react'
import { useTabAlivePool } from './useTabAlivePool'
import { useUISettingsStore } from '../stores/useUISettingsStore'

function resetSettings(overrides?: Partial<{ keepAliveCount: number; keepAlivePinned: boolean }>) {
  useUISettingsStore.setState({
    keepAliveCount: overrides?.keepAliveCount ?? 0,
    keepAlivePinned: overrides?.keepAlivePinned ?? false,
    terminalSettingsVersion: 0,
    terminalRevealDelay: 300,
    terminalRenderer: 'webgl',
  })
}

interface MockTab { id: string; pinned: boolean }

describe('useTabAlivePool', () => {
  beforeEach(() => resetSettings())

  it('keepAliveCount=0: pool only contains activeTabId', () => {
    const { result } = renderHook(() => useTabAlivePool('tab-a', [
      { id: 'tab-a', pinned: false },
      { id: 'tab-b', pinned: false },
    ] as MockTab[]))
    expect(result.current.aliveIds).toEqual(['tab-a'])
  })

  it('keepAliveCount=2: pool keeps last 2 visited tabs', () => {
    resetSettings({ keepAliveCount: 2 })
    const tabs = [
      { id: 'a', pinned: false },
      { id: 'b', pinned: false },
      { id: 'c', pinned: false },
    ] as MockTab[]

    const { result, rerender } = renderHook(
      ({ activeId }) => useTabAlivePool(activeId, tabs),
      { initialProps: { activeId: 'a' } },
    )

    act(() => rerender({ activeId: 'b' }))
    act(() => rerender({ activeId: 'c' }))
    // Pool: [c (active), b], a evicted
    expect(result.current.aliveIds).toContain('c')
    expect(result.current.aliveIds).toContain('b')
    expect(result.current.aliveIds).not.toContain('a')
  })

  it('keepAlivePinned=true: pinned tabs do not count toward pool limit', () => {
    resetSettings({ keepAliveCount: 1, keepAlivePinned: true })
    const tabs = [
      { id: 'pinned1', pinned: true },
      { id: 'normal1', pinned: false },
      { id: 'normal2', pinned: false },
    ] as MockTab[]

    const { result, rerender } = renderHook(
      ({ activeId }) => useTabAlivePool(activeId, tabs),
      { initialProps: { activeId: 'pinned1' } },
    )
    act(() => rerender({ activeId: 'normal1' }))
    act(() => rerender({ activeId: 'normal2' }))
    // Pool: [normal2 (active), pinned1 (exempt)], normal1 evicted
    expect(result.current.aliveIds).toContain('normal2')
    expect(result.current.aliveIds).toContain('pinned1')
    expect(result.current.aliveIds).not.toContain('normal1')
  })

  it('terminalSettingsVersion bump clears pool', () => {
    resetSettings({ keepAliveCount: 3 })
    const tabs = [{ id: 'a', pinned: false }, { id: 'b', pinned: false }] as MockTab[]

    const { result, rerender } = renderHook(
      ({ activeId }) => useTabAlivePool(activeId, tabs),
      { initialProps: { activeId: 'a' } },
    )
    act(() => rerender({ activeId: 'b' }))
    expect(result.current.aliveIds).toContain('a')

    // Bump version
    act(() => useUISettingsStore.getState().bumpTerminalSettingsVersion())
    act(() => rerender({ activeId: 'b' }))
    // Pool rebuilt: only active tab
    expect(result.current.aliveIds).toEqual(['b'])
    expect(result.current.poolVersion).toBe(1)
  })
})
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `cd spa && npx vitest run src/hooks/useTabAlivePool.test.ts`
Expected: FAIL — module 不存在

- [ ] **Step 3: 實作 useTabAlivePool**

`spa/src/hooks/useTabAlivePool.ts`：

```typescript
import { useEffect, useRef, useMemo } from 'react'
import { useUISettingsStore } from '../stores/useUISettingsStore'

interface MinimalTab {
  id: string
  pinned: boolean
}

export function useTabAlivePool(activeTabId: string | null, tabs: MinimalTab[]) {
  const keepAliveCount = useUISettingsStore((s) => s.keepAliveCount)
  const keepAlivePinned = useUISettingsStore((s) => s.keepAlivePinned)
  const settingsVersion = useUISettingsStore((s) => s.terminalSettingsVersion)

  // LRU history: most-recent-first
  const historyRef = useRef<string[]>([])
  const prevVersionRef = useRef(settingsVersion)

  // Clear history on settings version bump
  if (settingsVersion !== prevVersionRef.current) {
    historyRef.current = []
    prevVersionRef.current = settingsVersion
  }

  // Update LRU history when activeTabId changes
  useEffect(() => {
    if (!activeTabId) return
    const h = historyRef.current
    const idx = h.indexOf(activeTabId)
    if (idx > 0) h.splice(idx, 1)
    if (idx !== 0) h.unshift(activeTabId)
  }, [activeTabId])

  const tabMap = useMemo(() => new Map(tabs.map((t) => [t.id, t])), [tabs])

  const aliveIds = useMemo(() => {
    const h = historyRef.current
    if (keepAliveCount === 0) {
      return activeTabId ? [activeTabId] : []
    }

    const alive: string[] = []
    const pinnedAlive: string[] = []
    let normalCount = 0

    for (const id of h) {
      const tab = tabMap.get(id)
      if (!tab) continue
      if (keepAlivePinned && tab.pinned) {
        pinnedAlive.push(id)
      } else {
        if (normalCount < keepAliveCount + 1) { // +1 for active tab
          alive.push(id)
          normalCount++
        }
      }
    }
    return [...pinnedAlive, ...alive]
  }, [activeTabId, keepAliveCount, keepAlivePinned, tabMap, settingsVersion])

  return { aliveIds, poolVersion: settingsVersion }
}
```

- [ ] **Step 4: 執行測試確認通過**

Run: `cd spa && npx vitest run src/hooks/useTabAlivePool.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add spa/src/hooks/useTabAlivePool.ts spa/src/hooks/useTabAlivePool.test.ts
git commit -m "feat(hooks): add useTabAlivePool LRU keep-alive pool"
```

### Task 2.3: TabContent 改為 pool 渲染

- [ ] **Step 1: 擴充 TabContent 測試**

在 `spa/src/components/TabContent.test.tsx` 加入 keep-alive 測試：

```typescript
describe('keep-alive pool rendering', () => {
  it('renders only activeTab when keepAliveCount=0', () => {
    // Default behavior — same as existing tests
    render(<TabContent activeTab={sessionTab} allTabs={[sessionTab, editorTab]} wsBase="ws://x" daemonBase="http://x" />)
    expect(screen.getByTestId('session-renderer')).toBeInTheDocument()
  })

  it('keeps hidden tabs in DOM when keepAliveCount > 0', () => {
    useUISettingsStore.setState({ keepAliveCount: 2 })
    // After switching from tab A to tab B, A should still be in DOM but hidden
    // (Implementation detail: test via data-testid presence)
  })
})
```

- [ ] **Step 2: 改寫 TabContent.tsx**

`spa/src/components/TabContent.tsx` — 使用 pool 渲染：

```typescript
import { getTabRenderer } from '../lib/tab-registry'
import { useTabAlivePool } from '../hooks/useTabAlivePool'
import { useUISettingsStore } from '../stores/useUISettingsStore'
import type { Tab } from '../types/tab'

interface Props {
  activeTab: Tab | null
  allTabs: Tab[]
  wsBase: string
  daemonBase: string
}

export function TabContent({ activeTab, allTabs, wsBase, daemonBase }: Props) {
  const { aliveIds, poolVersion } = useTabAlivePool(
    activeTab?.id ?? null,
    allTabs.map((t) => ({ id: t.id, pinned: t.pinned })),
  )

  if (!activeTab && aliveIds.length === 0) {
    return (
      <div className="flex-1 flex items-center justify-center text-gray-600 text-sm">
        選擇或建立一個分頁開始使用
      </div>
    )
  }

  // Build tab lookup for alive IDs
  const tabMap = new Map(allTabs.map((t) => [t.id, t]))

  return (
    <div className="flex-1 relative">
      {aliveIds.map((id) => {
        const tab = tabMap.get(id)
        if (!tab) return null
        const config = getTabRenderer(tab.type)
        if (!config) return null
        const Renderer = config.component
        const isActive = id === activeTab?.id
        return (
          <div
            key={`${id}-${poolVersion}`}
            style={{ display: isActive ? 'contents' : 'none' }}
          >
            <Renderer tab={tab} isActive={isActive} wsBase={wsBase} daemonBase={daemonBase} />
          </div>
        )
      })}
    </div>
  )
}
```

- [ ] **Step 2b: 更新既有 TabContent.test.tsx**

既有測試 `<TabContent activeTab={...} wsBase="..." daemonBase="..." />` 缺少 `allTabs` prop。更新所有測試呼叫，加入 `allTabs` 陣列（包含 activeTab 本身）。例如：

```tsx
// 舊
<TabContent activeTab={sessionTab} wsBase="ws://test" daemonBase="http://test" />
// 新
<TabContent activeTab={sessionTab} allTabs={[sessionTab]} wsBase="ws://test" daemonBase="http://test" />
```

- [ ] **Step 3: 更新 App.tsx 傳入 allTabs**

`spa/src/App.tsx` 的 `<TabContent>` 調用改為：

```tsx
<TabContent
  activeTab={activeTab ?? null}
  allTabs={tabOrder.map((id) => tabs[id]).filter(Boolean)}
  wsBase={wsBase}
  daemonBase={daemonBase}
/>
```

同時移除 `onTerminalReconnect={() => {}}`（SettingsPanel 改用 bumpTerminalSettingsVersion）。

- [ ] **Step 4: 更新 SettingsPanel — 使用 bumpTerminalSettingsVersion**

`spa/src/components/SettingsPanel.tsx`：
- 移除 `onTerminalReconnect` prop
- 在儲存 terminal 相關設定後呼叫 `useUISettingsStore.getState().bumpTerminalSettingsVersion()`

- [ ] **Step 5: 執行全部測試**

Run: `cd spa && npx vitest run`
Expected: ALL PASS

- [ ] **Step 6: Commit**

```bash
git add spa/src/components/TabContent.tsx spa/src/App.tsx spa/src/components/SettingsPanel.tsx
git commit -m "feat(tab-content): LRU keep-alive pool rendering + remove onTerminalReconnect prop"
```

---

## Task 3: 拖曳排序

**Branch:** `feat/phase1.5-drag-sort`（基於 Task 1 合併後）

**Files:**
- Create: `spa/src/components/SortableTab.tsx`
- Modify: `spa/src/components/TabBar.tsx`（重寫）
- Modify: `spa/package.json`（加 dnd-kit）
- Test: `spa/src/components/TabBar.test.tsx`（重寫）

### Task 3.1: 安裝 dnd-kit

- [ ] **Step 1: 安裝依賴**

Run: `cd spa && pnpm add @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities`

- [ ] **Step 2: Commit**

```bash
git add spa/package.json spa/pnpm-lock.yaml
git commit -m "deps: add @dnd-kit/core + sortable + utilities"
```

### Task 3.2: SortableTab 元件

- [ ] **Step 1: 建立 SortableTab.tsx**

`spa/src/components/SortableTab.tsx`：

```typescript
import { useSortable } from '@dnd-kit/sortable'
import { CSS } from '@dnd-kit/utilities'
import { X, Lock } from '@phosphor-icons/react'
import type { Tab } from '../types/tab'
import { getTabIcon } from '../lib/tab-registry'
import { isDirty } from '../lib/tab-helpers'

const ICON_MAP: Record<string, React.ComponentType<{ size: number; className?: string }>> = {}
// Will be populated by TabBar parent (passed via context or imported)

interface Props {
  tab: Tab
  isActive: boolean
  pinned?: boolean
  onSelect: (tabId: string) => void
  onClose: (tabId: string) => void
  onMiddleClick: (tabId: string) => void
  onContextMenu: (e: React.MouseEvent, tabId: string) => void
  iconMap: Record<string, React.ComponentType<{ size: number; className?: string }>>
}

export function SortableTab({ tab, isActive, pinned, onSelect, onClose, onMiddleClick, onContextMenu, iconMap }: Props) {
  const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({ id: tab.id })

  const style = {
    transform: CSS.Transform.toString(transform),
    transition,
    opacity: isDragging ? 0.5 : 1,
  }

  const iconName = getTabIcon(tab)
  const IconComponent = iconMap[iconName]

  if (pinned) {
    return (
      <button
        ref={setNodeRef}
        style={style}
        {...attributes}
        {...listeners}
        onClick={() => onSelect(tab.id)}
        onMouseUp={(e) => { if (e.button === 1) { e.preventDefault(); onMiddleClick(tab.id) } }}
        onContextMenu={(e) => { e.preventDefault(); onContextMenu(e, tab.id) }}
        className={`flex items-center justify-center w-9 h-full cursor-pointer transition-colors ${
          isActive ? 'text-white border-b-2 border-purple-400' : 'text-gray-500 hover:text-gray-300'
        }`}
        title={tab.label}
      >
        {IconComponent && <IconComponent size={14} className="flex-shrink-0" />}
        {tab.locked && !tab.pinned && <Lock size={8} className="absolute top-0.5 right-0.5 text-gray-600" />}
      </button>
    )
  }

  return (
    <button
      ref={setNodeRef}
      style={style}
      {...attributes}
      {...listeners}
      onClick={() => onSelect(tab.id)}
      onMouseUp={(e) => { if (e.button === 1) { e.preventDefault(); onMiddleClick(tab.id) } }}
      onContextMenu={(e) => { e.preventDefault(); onContextMenu(e, tab.id) }}
      className={`group flex items-center gap-1.5 px-3 h-full text-xs whitespace-nowrap cursor-pointer transition-colors ${
        isActive ? 'text-white border-b-2 border-purple-400' : 'text-gray-500 hover:text-gray-300'
      }`}
    >
      {IconComponent && <IconComponent size={14} className="flex-shrink-0" />}
      <span>{tab.label}</span>
      {isDirty(tab) && <span className="text-amber-400 text-[10px]">●</span>}
      {tab.locked && <Lock size={10} className="text-gray-600 ml-0.5" />}
      {!tab.locked && (
        <span
          title="關閉分頁"
          onClick={(e) => { e.stopPropagation(); onClose(tab.id) }}
          className="ml-1 opacity-0 group-hover:opacity-100 hover:text-red-400 transition-opacity"
        >
          <X size={12} />
        </span>
      )}
    </button>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add spa/src/components/SortableTab.tsx
git commit -m "feat(ui): add SortableTab component for dnd-kit"
```

### Task 3.3: 重寫 TabBar 整合 dnd-kit

- [ ] **Step 1: 重寫 TabBar.test.tsx**

```typescript
import { describe, it, expect, beforeEach, vi } from 'vitest'
import { render, screen, fireEvent, cleanup } from '@testing-library/react'
import { TabBar } from './TabBar'
import { registerTabRenderer, clearRegistry } from '../lib/tab-registry'
import type { Tab } from '../types/tab'

beforeEach(() => {
  cleanup()
  clearRegistry()
  registerTabRenderer('session', {
    component: () => null,
    icon: () => 'Terminal',
    viewModes: ['terminal', 'stream'],
    defaultViewMode: 'terminal',
  })
})

const baseTabs: Tab[] = [
  { id: '1', type: 'session', label: 'pinned-a', icon: 'Terminal', hostId: 'local', viewMode: 'terminal', data: { sessionName: 'a' }, pinned: true, locked: true },
  { id: '2', type: 'session', label: 'normal-b', icon: 'Terminal', hostId: 'local', viewMode: 'terminal', data: { sessionName: 'b' }, pinned: false, locked: false },
  { id: '3', type: 'session', label: 'normal-c', icon: 'Terminal', hostId: 'local', viewMode: 'terminal', data: { sessionName: 'c' }, pinned: false, locked: false },
]

describe('TabBar', () => {
  it('renders pinned tabs as icon-only', () => {
    render(<TabBar tabs={baseTabs} activeTabId="2" onSelectTab={vi.fn()} onCloseTab={vi.fn()} onAddTab={vi.fn()} onReorderTabs={vi.fn()} onMiddleClick={vi.fn()} onContextMenu={vi.fn()} />)
    // Pinned tab should show title tooltip but not label text
    const pinnedBtn = screen.getByTitle('pinned-a')
    expect(pinnedBtn).toBeInTheDocument()
  })

  it('renders normal tabs with label', () => {
    render(<TabBar tabs={baseTabs} activeTabId="2" onSelectTab={vi.fn()} onCloseTab={vi.fn()} onAddTab={vi.fn()} onReorderTabs={vi.fn()} onMiddleClick={vi.fn()} onContextMenu={vi.fn()} />)
    expect(screen.getByText('normal-b')).toBeInTheDocument()
    expect(screen.getByText('normal-c')).toBeInTheDocument()
  })

  it('calls onSelectTab on click', () => {
    const onSelect = vi.fn()
    render(<TabBar tabs={baseTabs} activeTabId="2" onSelectTab={onSelect} onCloseTab={vi.fn()} onAddTab={vi.fn()} onReorderTabs={vi.fn()} onMiddleClick={vi.fn()} onContextMenu={vi.fn()} />)
    fireEvent.click(screen.getByText('normal-c'))
    expect(onSelect).toHaveBeenCalledWith('3')
  })

  it('calls onAddTab on + button', () => {
    const onAdd = vi.fn()
    render(<TabBar tabs={baseTabs} activeTabId="2" onSelectTab={vi.fn()} onCloseTab={vi.fn()} onAddTab={onAdd} onReorderTabs={vi.fn()} onMiddleClick={vi.fn()} onContextMenu={vi.fn()} />)
    fireEvent.click(screen.getByTitle('新增分頁'))
    expect(onAdd).toHaveBeenCalled()
  })

  it('calls onCloseTab via close button on unlocked tab', () => {
    const onClose = vi.fn()
    render(<TabBar tabs={baseTabs} activeTabId="2" onSelectTab={vi.fn()} onCloseTab={onClose} onAddTab={vi.fn()} onReorderTabs={vi.fn()} onMiddleClick={vi.fn()} onContextMenu={vi.fn()} />)
    const closeButtons = screen.getAllByTitle('關閉分頁')
    fireEvent.click(closeButtons[0])
    expect(onClose).toHaveBeenCalled()
  })

  it('locked tab hides close button', () => {
    const lockedTabs = [
      { ...baseTabs[1], locked: true },
    ]
    render(<TabBar tabs={lockedTabs} activeTabId="2" onSelectTab={vi.fn()} onCloseTab={vi.fn()} onAddTab={vi.fn()} onReorderTabs={vi.fn()} onMiddleClick={vi.fn()} onContextMenu={vi.fn()} />)
    expect(screen.queryByTitle('關閉分頁')).not.toBeInTheDocument()
  })

  it('shows dirty indicator', () => {
    const dirtyTab: Tab[] = [
      { id: '4', type: 'editor', label: 'dirty-file', icon: 'File', hostId: 'local', data: { filePath: '/x', isDirty: true }, pinned: false, locked: false },
    ]
    render(<TabBar tabs={dirtyTab} activeTabId="4" onSelectTab={vi.fn()} onCloseTab={vi.fn()} onAddTab={vi.fn()} onReorderTabs={vi.fn()} onMiddleClick={vi.fn()} onContextMenu={vi.fn()} />)
    expect(screen.getByText('●')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: 重寫 TabBar.tsx**

`spa/src/components/TabBar.tsx` — dnd-kit 雙區版：

```typescript
import { useCallback } from 'react'
import { DndContext, closestCenter, type DragEndEvent } from '@dnd-kit/core'
import { SortableContext, horizontalListSortingStrategy } from '@dnd-kit/sortable'
import { Plus } from '@phosphor-icons/react'
import { Terminal, ChatCircleDots, File as FileIcon } from '@phosphor-icons/react'
import { SortableTab } from './SortableTab'
import type { Tab } from '../types/tab'

const ICON_MAP: Record<string, React.ComponentType<{ size: number; className?: string }>> = {
  Terminal, ChatCircleDots, File: FileIcon,
}

interface Props {
  tabs: Tab[]
  activeTabId: string | null
  onSelectTab: (tabId: string) => void
  onCloseTab: (tabId: string) => void
  onAddTab: () => void
  onReorderTabs: (newOrder: string[]) => void
  onMiddleClick: (tabId: string) => void
  onContextMenu: (e: React.MouseEvent, tabId: string) => void
}

export function TabBar({ tabs, activeTabId, onSelectTab, onCloseTab, onAddTab, onReorderTabs, onMiddleClick, onContextMenu }: Props) {
  const pinnedTabs = tabs.filter((t) => t.pinned)
  const normalTabs = tabs.filter((t) => !t.pinned)
  const pinnedIds = pinnedTabs.map((t) => t.id)
  const normalIds = normalTabs.map((t) => t.id)

  const handleDragEnd = useCallback((event: DragEndEvent) => {
    const { active, over } = event
    if (!over || active.id === over.id) return

    const activeId = String(active.id)
    const overId = String(over.id)

    // Determine which zone the drag is in
    const inPinned = pinnedIds.includes(activeId)
    const overInPinned = pinnedIds.includes(overId)

    // Only allow same-zone reorder
    if (inPinned !== overInPinned) return

    const zone = inPinned ? [...pinnedIds] : [...normalIds]
    const oldIdx = zone.indexOf(activeId)
    const newIdx = zone.indexOf(overId)
    zone.splice(oldIdx, 1)
    zone.splice(newIdx, 0, activeId)

    // Reconstruct full order: pinned + normal
    const newOrder = inPinned ? [...zone, ...normalIds] : [...pinnedIds, ...zone]
    onReorderTabs(newOrder)
  }, [pinnedIds, normalIds, onReorderTabs])

  return (
    <div className="flex bg-[#12122a] border-b border-gray-800 h-9 items-center px-1 flex-shrink-0">
      <DndContext collisionDetection={closestCenter} onDragEnd={handleDragEnd}>
        {/* Pinned zone */}
        {pinnedTabs.length > 0 && (
          <>
            <SortableContext items={pinnedIds} strategy={horizontalListSortingStrategy}>
              <div className="flex gap-0.5 items-center h-full">
                {pinnedTabs.map((tab) => (
                  <SortableTab
                    key={tab.id}
                    tab={tab}
                    isActive={tab.id === activeTabId}
                    pinned
                    onSelect={onSelectTab}
                    onClose={onCloseTab}
                    onMiddleClick={onMiddleClick}
                    onContextMenu={onContextMenu}
                    iconMap={ICON_MAP}
                  />
                ))}
              </div>
            </SortableContext>
            <div className="w-px h-4 bg-gray-700 mx-1 flex-shrink-0" />
          </>
        )}

        {/* Normal zone */}
        <SortableContext items={normalIds} strategy={horizontalListSortingStrategy}>
          <div className="flex gap-0.5 items-center h-full min-w-0 overflow-hidden flex-1">
            {normalTabs.map((tab) => (
              <SortableTab
                key={tab.id}
                tab={tab}
                isActive={tab.id === activeTabId}
                onSelect={onSelectTab}
                onClose={onCloseTab}
                onMiddleClick={onMiddleClick}
                onContextMenu={onContextMenu}
                iconMap={ICON_MAP}
              />
            ))}
          </div>
        </SortableContext>
      </DndContext>

      <button
        onClick={onAddTab}
        className="flex items-center justify-center w-7 h-7 text-gray-600 hover:text-gray-400 cursor-pointer flex-shrink-0"
        title="新增分頁"
      >
        <Plus size={14} />
      </button>
    </div>
  )
}
```

- [ ] **Step 3: 更新 App.tsx 的 TabBar props**

```tsx
<TabBar
  tabs={displayTabs}
  activeTabId={activeTabId}
  onSelectTab={handleSelectTab}
  onCloseTab={handleCloseTab}
  onAddTab={handleAddTab}
  onReorderTabs={(order) => useTabStore.getState().reorderTabs(order)}
  onMiddleClick={(tabId) => {
    const tab = tabs[tabId]
    if (tab && !tab.locked) handleCloseTab(tabId)
  }}
  onContextMenu={(e, tabId) => {
    // Phase 1.5 Task 5 will implement this
    e.preventDefault()
  }}
/>
```

- [ ] **Step 4: 執行測試**

Run: `cd spa && npx vitest run`
Expected: ALL PASS

- [ ] **Step 5: Commit**

```bash
git add spa/src/components/TabBar.tsx spa/src/components/TabBar.test.tsx spa/src/App.tsx
git commit -m "feat(tab-bar): dnd-kit drag sorting with pinned/normal zones"
```

---

## Task 4: Tab 溢出箭頭

**Branch:** `feat/phase1.5-tab-overflow`（基於 Task 3 合併後）

**Files:**
- Create: `spa/src/hooks/useScrollOverflow.ts`
- Modify: `spa/src/components/TabBar.tsx`
- Test: `spa/src/hooks/useScrollOverflow.test.ts`

### Task 4.1: useScrollOverflow hook

- [ ] **Step 1: 寫測試**

`spa/src/hooks/useScrollOverflow.test.ts`：

```typescript
import { describe, it, expect } from 'vitest'
import { renderHook } from '@testing-library/react'
import { useScrollOverflow } from './useScrollOverflow'

describe('useScrollOverflow', () => {
  it('returns canScrollLeft=false, canScrollRight=false when ref is null', () => {
    const { result } = renderHook(() => useScrollOverflow())
    // containerRef.current is null by default
    expect(result.current.canScrollLeft).toBe(false)
    expect(result.current.canScrollRight).toBe(false)
  })
})
```

- [ ] **Step 2: 實作 hook**

`spa/src/hooks/useScrollOverflow.ts`：

```typescript
import { useRef, useState, useEffect, useCallback } from 'react'

export function useScrollOverflow() {
  const containerRef = useRef<HTMLDivElement>(null)
  const [canScrollLeft, setCanScrollLeft] = useState(false)
  const [canScrollRight, setCanScrollRight] = useState(false)

  const update = useCallback(() => {
    const el = containerRef.current
    if (!el) return
    setCanScrollLeft(el.scrollLeft > 0)
    setCanScrollRight(el.scrollLeft + el.clientWidth < el.scrollWidth - 1)
  }, [])

  useEffect(() => {
    const el = containerRef.current
    if (!el) return

    update()
    el.addEventListener('scroll', update, { passive: true })
    const observer = new ResizeObserver(update)
    observer.observe(el)

    return () => {
      el.removeEventListener('scroll', update)
      observer.disconnect()
    }
  }, [update])

  const scrollLeft = useCallback(() => {
    const el = containerRef.current
    if (!el) return
    el.scrollBy({ left: -150, behavior: 'smooth' })
  }, [])

  const scrollRight = useCallback(() => {
    const el = containerRef.current
    if (!el) return
    el.scrollBy({ left: 150, behavior: 'smooth' })
  }, [])

  return { containerRef, canScrollLeft, canScrollRight, scrollLeft, scrollRight }
}
```

- [ ] **Step 3: 執行測試確認通過**

Run: `cd spa && npx vitest run src/hooks/useScrollOverflow.test.ts`
Expected: PASS

- [ ] **Step 4: 整合到 TabBar**

在 TabBar 的 normal zone container 套用 `useScrollOverflow`，加左右 chevron 按鈕：

```typescript
import { CaretLeft, CaretRight } from '@phosphor-icons/react'
import { useScrollOverflow } from '../hooks/useScrollOverflow'

// Inside TabBar:
const { containerRef: normalZoneRef, canScrollLeft, canScrollRight, scrollLeft, scrollRight } = useScrollOverflow()

// Normal zone 改為:
<div className="relative flex-1 min-w-0">
  {canScrollLeft && (
    <button
      onClick={scrollLeft}
      className="absolute left-0 top-0 bottom-0 z-10 w-8 flex items-center justify-center bg-gradient-to-r from-[#12122a] to-transparent"
    >
      <CaretLeft size={14} className="text-gray-400" />
    </button>
  )}
  <div ref={normalZoneRef} className="flex gap-0.5 items-center h-full overflow-hidden">
    <SortableContext items={normalIds} strategy={horizontalListSortingStrategy}>
      {normalTabs.map(/* ... */)}
    </SortableContext>
  </div>
  {canScrollRight && (
    <button
      onClick={scrollRight}
      className="absolute right-0 top-0 bottom-0 z-10 w-8 flex items-center justify-center bg-gradient-to-l from-[#12122a] to-transparent"
    >
      <CaretRight size={14} className="text-gray-400" />
    </button>
  )}
</div>
```

- [ ] **Step 5: 執行全部測試 + lint**

Run: `cd spa && npx vitest run && npm run lint`
Expected: ALL PASS

- [ ] **Step 6: Commit**

```bash
git add spa/src/hooks/useScrollOverflow.ts spa/src/hooks/useScrollOverflow.test.ts spa/src/components/TabBar.tsx
git commit -m "feat(tab-bar): overflow scroll arrows with gradient fade"
```

---

## Task 5: 中鍵關閉 + 右鍵選單

**Branch:** `feat/phase1.5-context-menu`（基於 Task 1 合併後）

**Files:**
- Create: `spa/src/components/TabContextMenu.tsx`
- Create: `spa/src/components/TabContextMenu.test.tsx`
- Modify: `spa/src/App.tsx`

### Task 5.1: TabContextMenu 元件

- [ ] **Step 1: 寫測試**

`spa/src/components/TabContextMenu.test.tsx`：

```typescript
import { describe, it, expect, beforeEach, vi } from 'vitest'
import { render, screen, fireEvent, cleanup } from '@testing-library/react'
import { TabContextMenu } from './TabContextMenu'
import type { Tab } from '../types/tab'

const baseTab: Tab = {
  id: '1', type: 'session', label: 'test', icon: 'Terminal',
  hostId: 'local', viewMode: 'terminal', data: { sessionName: 'test' },
  pinned: false, locked: false,
}

const baseProps = {
  tab: baseTab,
  position: { x: 100, y: 100 },
  onClose: vi.fn(),
  onAction: vi.fn(),
  hasOtherUnlocked: true,
  hasRightUnlocked: true,
  hasDismissedSessions: true,
}

describe('TabContextMenu', () => {
  beforeEach(() => { cleanup(); vi.clearAllMocks() })

  it('shows viewMode toggle for session tab', () => {
    render(<TabContextMenu {...baseProps} />)
    expect(screen.getByText('切換至 Stream')).toBeInTheDocument()
  })

  it('hides viewMode toggle for non-session tab', () => {
    render(<TabContextMenu {...baseProps} tab={{ ...baseTab, type: 'editor', viewMode: undefined }} />)
    expect(screen.queryByText('切換至 Stream')).not.toBeInTheDocument()
  })

  it('shows 鎖定分頁 for unlocked tab', () => {
    render(<TabContextMenu {...baseProps} />)
    expect(screen.getByText('鎖定分頁')).toBeInTheDocument()
  })

  it('shows 解鎖分頁 for locked non-pinned tab', () => {
    render(<TabContextMenu {...baseProps} tab={{ ...baseTab, locked: true }} />)
    expect(screen.getByText('解鎖分頁')).toBeInTheDocument()
  })

  it('hides 解鎖分頁 for pinned tab', () => {
    render(<TabContextMenu {...baseProps} tab={{ ...baseTab, pinned: true, locked: true }} />)
    expect(screen.queryByText('解鎖分頁')).not.toBeInTheDocument()
  })

  it('關閉分頁 is disabled when locked', () => {
    render(<TabContextMenu {...baseProps} tab={{ ...baseTab, locked: true }} />)
    const closeItem = screen.getByText('關閉分頁')
    expect(closeItem.closest('button')).toHaveClass('opacity-40')
  })

  it('calls onAction with correct action', () => {
    render(<TabContextMenu {...baseProps} />)
    fireEvent.click(screen.getByText('鎖定分頁'))
    expect(baseProps.onAction).toHaveBeenCalledWith('lock')
  })

  it('shows 重新開啟已關閉的分頁 when hasDismissedSessions', () => {
    render(<TabContextMenu {...baseProps} />)
    expect(screen.getByText('重新開啟已關閉的分頁')).toBeInTheDocument()
  })

  it('hides 重新開啟已關閉的分頁 when no dismissed sessions', () => {
    render(<TabContextMenu {...baseProps} hasDismissedSessions={false} />)
    expect(screen.queryByText('重新開啟已關閉的分頁')).not.toBeInTheDocument()
  })
})
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `cd spa && npx vitest run src/components/TabContextMenu.test.tsx`
Expected: FAIL — module 不存在

- [ ] **Step 3: 實作 TabContextMenu**

`spa/src/components/TabContextMenu.tsx`：

```typescript
import { useEffect, useRef } from 'react'
import type { Tab } from '../types/tab'

export type ContextMenuAction =
  | 'viewMode-terminal' | 'viewMode-stream'
  | 'lock' | 'unlock' | 'pin' | 'unpin'
  | 'close' | 'closeOthers' | 'closeRight'
  | 'reopenClosed'

interface Props {
  tab: Tab
  position: { x: number; y: number }
  onClose: () => void
  onAction: (action: ContextMenuAction) => void
  hasOtherUnlocked: boolean
  hasRightUnlocked: boolean
  hasDismissedSessions: boolean
}

interface MenuItem {
  label: string
  action: ContextMenuAction
  show: boolean
  disabled?: boolean
}

export function TabContextMenu({ tab, position, onClose, onAction, hasOtherUnlocked, hasRightUnlocked, hasDismissedSessions }: Props) {
  const ref = useRef<HTMLDivElement>(null)

  useEffect(() => {
    const handler = (e: MouseEvent) => {
      if (ref.current && !ref.current.contains(e.target as Node)) onClose()
    }
    const escHandler = (e: KeyboardEvent) => { if (e.key === 'Escape') onClose() }
    document.addEventListener('mousedown', handler)
    document.addEventListener('keydown', escHandler)
    return () => {
      document.removeEventListener('mousedown', handler)
      document.removeEventListener('keydown', escHandler)
    }
  }, [onClose])

  const isSession = tab.type === 'session'
  const items: (MenuItem | 'separator')[] = [
    // ViewMode section
    ...(isSession && tab.viewMode !== 'terminal' ? [{ label: '切換至 Terminal', action: 'viewMode-terminal' as const, show: true }] : []),
    ...(isSession && tab.viewMode !== 'stream' ? [{ label: '切換至 Stream', action: 'viewMode-stream' as const, show: true }] : []),
    ...(isSession ? ['separator' as const] : []),
    // Lock/Pin section
    { label: '鎖定分頁', action: 'lock' as const, show: !tab.locked },
    { label: '解鎖分頁', action: 'unlock' as const, show: tab.locked && !tab.pinned },
    { label: '固定分頁', action: 'pin' as const, show: !tab.pinned },
    { label: '取消固定', action: 'unpin' as const, show: tab.pinned },
    'separator',
    // Close section
    { label: '關閉分頁', action: 'close' as const, show: true, disabled: tab.locked },
    { label: '關閉其他分頁', action: 'closeOthers' as const, show: hasOtherUnlocked },
    { label: '關閉右側分頁', action: 'closeRight' as const, show: hasRightUnlocked },
    'separator',
    // Reopen
    { label: '重新開啟已關閉的分頁', action: 'reopenClosed' as const, show: hasDismissedSessions },
  ]

  const visibleItems = items.filter((item) => item === 'separator' || item.show)
  // Remove leading/trailing/consecutive separators
  const cleaned: typeof visibleItems = []
  for (const item of visibleItems) {
    if (item === 'separator') {
      if (cleaned.length > 0 && cleaned[cleaned.length - 1] !== 'separator') cleaned.push(item)
    } else {
      cleaned.push(item)
    }
  }
  if (cleaned[cleaned.length - 1] === 'separator') cleaned.pop()

  return (
    <div
      ref={ref}
      className="fixed z-50 bg-[#1e1e2e] border border-gray-700 rounded-lg shadow-xl py-1 min-w-[200px] text-xs"
      style={{ left: position.x, top: position.y }}
    >
      {cleaned.map((item, i) => {
        if (item === 'separator') {
          return <div key={`sep-${i}`} className="border-t border-gray-700 my-1" />
        }
        return (
          <button
            key={item.action}
            onClick={() => { if (!item.disabled) { onAction(item.action); onClose() } }}
            className={`w-full text-left px-3 py-1.5 hover:bg-[#2a2a3e] transition-colors ${
              item.disabled ? 'opacity-40 cursor-not-allowed' : 'cursor-pointer'
            }`}
          >
            {item.label}
          </button>
        )
      })}
    </div>
  )
}
```

- [ ] **Step 4: 執行測試確認通過**

Run: `cd spa && npx vitest run src/components/TabContextMenu.test.tsx`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add spa/src/components/TabContextMenu.tsx spa/src/components/TabContextMenu.test.tsx
git commit -m "feat(ui): add TabContextMenu with lock/pin/close/reopen actions"
```

### Task 5.2: 整合到 App.tsx

- [ ] **Step 1: App.tsx 加入 context menu 狀態與 handler**

```typescript
import { TabContextMenu, type ContextMenuAction } from './components/TabContextMenu'

// State
const [contextMenu, setContextMenu] = useState<{ tab: Tab; position: { x: number; y: number } } | null>(null)

// Handler
const handleContextMenu = useCallback((e: React.MouseEvent, tabId: string) => {
  e.preventDefault()
  const tab = tabs[tabId]
  if (tab) setContextMenu({ tab, position: { x: e.clientX, y: e.clientY } })
}, [tabs])

const handleContextAction = useCallback((action: ContextMenuAction) => {
  if (!contextMenu) return
  const { tab } = contextMenu
  const store = useTabStore.getState()
  switch (action) {
    case 'viewMode-terminal': store.setViewMode(tab.id, 'terminal'); break
    case 'viewMode-stream': store.setViewMode(tab.id, 'stream'); break
    case 'lock': store.lockTab(tab.id); break
    case 'unlock': store.unlockTab(tab.id); break
    case 'pin': store.pinTab(tab.id); break
    case 'unpin': store.unpinTab(tab.id); break
    case 'close': handleCloseTab(tab.id); break
    case 'closeOthers': {
      const toClose = tabOrder.filter((id) => id !== tab.id && !tabs[id]?.locked)
      toClose.forEach((id) => handleCloseTab(id))
      break
    }
    case 'closeRight': {
      const idx = tabOrder.indexOf(tab.id)
      const toClose = tabOrder.slice(idx + 1).filter((id) => !tabs[id]?.locked)
      toClose.forEach((id) => handleCloseTab(id))
      break
    }
    case 'reopenClosed': {
      const dismissed = store.dismissedSessions
      if (dismissed.length > 0) store.undismissSession(dismissed[dismissed.length - 1])
      break
    }
  }
}, [contextMenu, tabs, tabOrder, handleCloseTab])
```

更新 TabBar props：

```tsx
onContextMenu={handleContextMenu}
```

在 JSX 最後加 context menu overlay：

```tsx
{contextMenu && (
  <TabContextMenu
    tab={contextMenu.tab}
    position={contextMenu.position}
    onClose={() => setContextMenu(null)}
    onAction={handleContextAction}
    hasOtherUnlocked={tabOrder.some((id) => id !== contextMenu.tab.id && !tabs[id]?.locked)}
    hasRightUnlocked={tabOrder.slice(tabOrder.indexOf(contextMenu.tab.id) + 1).some((id) => !tabs[id]?.locked)}
    hasDismissedSessions={useTabStore.getState().dismissedSessions.length > 0}
  />
)}
```

- [ ] **Step 2: 執行全部測試 + lint + build**

Run: `cd spa && npx vitest run && npm run lint && npm run build`
Expected: ALL PASS

- [ ] **Step 3: Commit**

```bash
git add spa/src/App.tsx
git commit -m "feat(app): integrate TabContextMenu with all actions"
```

---

## 最終驗證

- [ ] **全部測試**: `cd spa && npx vitest run`
- [ ] **Lint**: `cd spa && npm run lint`
- [ ] **Build**: `cd spa && npm run build`
- [ ] **手動測試清單**:
  - 拖曳排序：pinned 區內排序、normal 區內排序、跨區拖曳被擋
  - 右鍵選單：所有 10 個選單項目逐一測試
  - 中鍵關閉：normal tab 可關、locked tab 不可關
  - 固定/鎖定：pin → icon-only、unpin → 恢復、lock → 擋關閉、unlock → 可關閉
  - 溢出箭頭：開 5+ tab 後出現右箭頭、捲動後出現左箭頭
  - Keep-alive：設 keepAliveCount=2，切 tab 後切回不閃 overlay
  - Settings 變更：改 renderer 後所有 terminal 重建
